# Exposes Book-E dashboard for Cloudflare Tunnel access
apiVersion: v1
kind: Service
metadata:
  name: book-e
  namespace: automate-e
spec:
  selector:
    app: book-e
  ports:
    - name: dashboard
      port: 3000
      targetPort: 3000
